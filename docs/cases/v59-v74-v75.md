# Case: v59 → v74 → v75, keep the useful direction and correct the failure

## Specific problem

The title must relate to the subject without covering important visual material or becoming a decorative curve with no reading logic.

## Input

- `assets/iteration-panels/ai-cover-title-layout-comparison-sculpt-v59.jpg`
- `assets/iteration-panels/ai-cover-title-layout-comparison-sculpt-v74.jpg`
- `assets/iteration-panels/ai-cover-title-layout-comparison-sculpt-v75.jpg`
- Matching debug boards in `assets/debug-panels/`.

These are repository process assets, not posts or platform records.

## Method

1. Preserve v59's direction: less framing, straighter text axes, clearer hierarchy, and subject avoidance.
2. Inspect v74 as a negative result: a meaningful contour can support a main-title curve, but side/lower labels have no such justification.
3. In v75, keep curvature only where a head/task contour provides a reason; restore side/lower copy to straight, aligned reading paths.
4. Record the rule and retain all three boards so the correction remains auditable.

## Reproducible output

```sh
node scripts/check-package.mjs
```

Then inspect the three listed boards and their debug counterparts. The output to reproduce is a locally verified package plus a documented design decision, not a social-media result.

## Evidence

The versioned ordinary boards show what changed; debug boards show the checked zones; the written iteration story records why the rule changed. Together they support a narrow claim: this repository preserved and reviewed a sequence of layout decisions.

## Unsupported conclusions

The evidence does not show that any image was posted, passed platform review, gained reach/engagement, improved growth, or works for another topic or audience.

## Three-minute exercise

Run the command, open v59/v74/v75, and write one sentence for each: “useful direction,” “failure,” and “repair rule.” If you cannot identify all three, do not turn the rule into a production instruction.
